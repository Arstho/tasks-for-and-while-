let arr = ['JS', 'CSS', 'PHP', 'С', 'С++', 'Pascal', 'java', 'SQL', 'Python', 'Delphi'];

let i = 0;
while (i < arr.length) {
    if (arr[i].length > 3) {
        console.log(arr[i]);
    }
    i++;
}